# Welles inference

- **`modal_app.py`** — GPU API on [Modal](https://modal.com) (recommended; no HF Pro)
- **`app.py`** — Gradio Space (needs HF paid GPU / ZeroGPU)

Vercel calls Modal via `WELLES_API_URL`. See `../web/README.md`.
